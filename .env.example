# Zero Token Gateway 地址（NAS 上的 Zero Token 服务）
ZERO_TOKEN_URL=http://localhost:3002

# Zero Token Gateway 的 Token（在 openclaw.json 中配置）
ZERO_TOKEN_KEY=your-gateway-token

# 后端端口
PORT=3000

# 免费用户每日限制次数
RATE_LIMIT_DAILY=20
